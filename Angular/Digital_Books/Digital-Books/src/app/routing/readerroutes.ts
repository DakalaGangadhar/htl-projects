import { MyOrderComponent } from "../reader/my-order/my-order.component";
import { ReaderComponent } from "../reader/reader/reader.component";

export const readerroutes = [
    { path: 'add', component: ReaderComponent }
    //{path:'myorder', component:MyOrderComponent}
];
